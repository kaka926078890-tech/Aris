---
description: 文档命名与项目规范
globs: ["**/*.md", "**/*.mdc", "docs/**", "memory/**"]
alwaysApply: false
---

# 文档命名与项目规范

## 文档命名

- **所有文档文件**（含 `docs/`、各子项目下的文档目录及 `.md`、`.mdc` 等）统一使用 **小写英文字母 + 下划线** 命名。
- 格式：`lowercase_with_underscores.md`，禁止使用大写、连字符或驼峰。
- 示例：
  - ✅ `project_layout.md`、`record_mechanism_improvement.md`、`distribution_embedding_options.md`
  - ❌ `PROJECT-LAYOUT.md`、`RecordMechanismImprovement.md`、`distribution-embedding-options.md`
- 新建或重命名文档时一律遵循此规范。

## 项目约定（当前代码库）

- **前端**：`src/renderer/` 或 v2 `apps/renderer/` 为界面入口；v2 使用 React + Tailwind。
- **主进程**：Electron 主进程入口为 `main.js`；预加载通过 `window.aris` 暴露 IPC。
- **模块**：v2 下 `packages/server`、`packages/store`、`packages/config`；CommonJS，无 TypeScript。
- **配置与密钥**：环境变量用 `dotenv`，不提交 `.env`；API 等从 `process.env` 或运行时 config 读取。
- **文档**：设计/架构类文档放 `docs/`，命名采用小写_下划线。
