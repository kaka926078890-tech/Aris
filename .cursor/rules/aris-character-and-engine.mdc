---
description: Aris 人格与裁量优先；引擎层规则保持薄；策略配置是运行参数而非替角色下判断
globs:
alwaysApply: true
---

# Aris：人格 / 策略 / 「硬规则」的边界

面向 **陪伴型、有自主裁量** 的产品取向时，在设计与实现上遵守以下分层，避免把 Aris 做成「条例驱动」的 bot。

## 1. 心力放在哪里

- **优先投入**：`persona`、对话与记忆结构、工具与检索设计、主对话中 **角色如何推理与表达**。
- **引擎层**（Planner 开关、跳过策略、延迟闸门）：能薄则薄；**不为省一次 API** 而堆长关键词表或复杂 if-else 替用户/替角色做决定。

## 2. 配置文件 ≠ 耻辱，但要分清「旋钮」与「人格」

- 写在 JSON / env 里与写在代码里，对模型而言都可视为外生约束；差别在于 **可维护性、可关、可文档化**。
- **策略类配置**（如是否关闭 Planner、指标日志）应理解为 **运行环境与产品取舍的旋钮**，不是「用户守则」。
- **长列表禁止/必须** 容易显得强硬：能一句原则 + 少量示例则不要灌列表；长表放配置仅供维护或校验，**不默认铺满 system**（与 `prompt-and-tools-design.mdc` 一致）。

## 3. 性能优化 vs 自主性

- **Planner 闲聊跳过、关键词路由** 等：属于 **工程折中**，会削弱「每轮都由编排模型决定注入」的一致性。  
- **默认立场**：**不主动实现**「仅靠词表跳过 Planner」；若未来立项，须 **有 P0 类指标证明瓶颈**、**默认保守（宁可多跑 Planner）**、**可一键关闭**、词表可配置且 **不写死一大段中文在代码里**。
- **优先提供的延迟选项**：用户/环境 **显式关闭 Planner**（`ARIS_PROMPT_PLANNER_ENABLED=false` 或 behavior 配置），接受更大 system，而不是偷偷加规则。

## 4. 与现有规则的关系

- **上下文与工具**：仍遵守 `prompt-and-tools-design.mdc`（工具按需、注入最小化）。
- **方案陈述**：仍遵守 `solutions-mvp-and-final.mdc`；涉及「策略层」改动时，在文档中标出 **MVP / 最终** 与 **对产品人格的影响**。

## 5. 文档义务

- 新增或调整 **策略类** 行为（Planner 跳过、工具预算等）时，在 `v2/docs/` 对应优化/架构文档中写清 **产品原则与默认**，避免后人误以为「多上规则表 = 更智能」。
