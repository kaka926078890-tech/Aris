---
description: 新增可配置环境变量时须同步提供本机/本地示例
globs:
alwaysApply: true
---

# 环境变量须提供本机示例

当新增**需要用户配置的环境变量**时，必须同步在项目内提供「本机地址/本地开发」的示例配置，便于在本机直接运行与测试。

- **位置**：在 `v2/.env.example` 中为每个新变量增加一行，并给出适合本机开发的示例值或注释（如 `ARIS_V2_DATA_DIR=./data`、`ENABLE_WEB_FETCH=true`）。
- 若某变量仅在生产或特定环境使用，可用注释标出「本机可省略」或「本机示例：xxx」。
- 目的：任何人 clone 后按 .env.example 复制为 .env 并填本机值即可跑通，无需再查文档拼变量名。
