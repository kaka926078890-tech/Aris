---
description: 动态信息优先用工具按需获取，固定提示词保持极简；仅在性能或效果明显更好时采用注入或代码侧逻辑，并需说明理由
globs:
alwaysApply: true
---

# 提示词与工具设计习惯

新增「需要模型使用的上下文信息」时，按以下优先级设计，避免固定 system prompt 持续膨胀。

## 1. 优先：工具按需获取（推荐默认）

- **做法**：数据存 store，不写入每轮 system；在 system 里只留**一句说明**，例如「用户有记录的喜好与习惯，需要时可调用 get_preferences 获取」。
- **适用**：喜好、习惯、偏好列表、可选的关联/历史等——模型在需要时再调工具拿，拿多少用多少。
- **优点**：固定 prompt 不随数据增长而变长；模型按对话需要取数，token 更省、也减少无关信息干扰。

## 2. 其次：代码侧逻辑（不经过模型）

- **做法**：行为由代码直接决定，不把数据塞进 prompt、也不让模型调工具决定。
- **适用**：例如「用户近期表示过累/想安静 → 本轮回不发主动消息」：在 proactive 里读 `last_tired_or_quiet_at`，超时则 return null，**无需**在 prompt 里写「用户刚说累」。
- **优点**：确定性强、无额外 token、无模型误判；适合明确规则类行为。

## 3. 慎用：固定注入进 system

- **做法**：在 buildPromptContext 里拼出一段内容，写入 CONTEXT_TEMPLATE 的占位符。
- **适用**：仅当（a）**首条回复就必须用到**且模型来不及先调工具，或（b）**每次对话都必然用到、且内容极短**（如 1～2 句摘要）时，才考虑注入。
- **要求**：若采用注入，必须**严格限制长度**（行数/字数上限、配置化），并注明理由；新增占位符需有充分理由，否则默认改为「一句提示 + get_xxx 工具」。

## 4. 禁止用语 / 规则类

- **做法**：在 persona/rules 里用**一句话 + 少量示例**（如 2～3 个）说明，不把整份列表灌进每轮 prompt；长列表放配置文件供人工维护或后续校验用。

## 5. 当有更优方案时的义务

- 若在**性能**（如少一轮工具调用、降低延迟）或**效果**（如首句就必须带出某信息、模型不调工具会明显漏用）上，存在比「工具按需获取」更好的做法（例如极少量注入、或纯代码逻辑），应在设计时**主动提出并写清理由**，而不是默认选工具或默认注入。

总结：**能工具就工具，能代码就代码；固定提示词只做最少必要注入，并说明为何不交给工具/代码。**

## 6. Aris 取向：策略层勿抢人格的戏

- **人格与裁量**（怎么答、记什么、何时用工具）优先放在 persona、记忆与主对话；**引擎策略**（如是否跳过编排 LLM）保持 **可关、可文档、默认保守**，避免用长关键词表替角色做「闲聊/非闲聊」判决，除非有明确指标与文档立项。详见 `aris-character-and-engine.mdc`。
